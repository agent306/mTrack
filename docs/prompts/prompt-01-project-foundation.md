# Prompt 01: Project Foundation

```text
Build the mTrack foundation.

Read all docs first. Create the Laravel/PHP + Inertia + Vue + TypeScript + Tailwind + Headless UI + Lucide web app foundation inside `api/`, plus PostgreSQL, Redis, Horizon, and Reverb setup. Create the Flutter app foundation inside `mobile/`. Preserve the existing `api/nixpacks.toml`.

Implement only the foundation: auth scaffolding, tenant-aware base models, app layout shell, routing structure, config, queues, and deployment readiness. Do not build feature pages yet.

For frontend shell/design decisions, use Stitch MCP and load the frontend-design skill. Use `docs/DESIGN_SYSTEM.md` as the source for visual tokens.
```
