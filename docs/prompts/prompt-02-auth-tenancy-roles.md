# Prompt 02: Auth, Tenancy, Roles

```text
Implement mTrack authentication, tenancy, and authorization.

Use passwordless email magic links and Google sign-in. No password auth. Implement hybrid tenant-aware data model, custom roles, and Hide/View/Edit permissions per module and per fleet/group.

Read `docs/DECISIONS.md`, `docs/DATA_MODEL.md`, and `docs/SECURITY.md`. Build policies/middleware/tests that prevent cross-tenant access.

Use Stitch MCP for any auth/admin UI decisions and frontend-design for all frontend work.
```
