# Prompt 04: Ingestion Pipeline

```text
Build the mTrack ingestion pipeline.

Support any HTTP body. Store raw payloads before normalization. Implement developer-managed internal PHP parser contract classes. No runtime user-uploaded parser code. Start unsigned ingestion with parser-defined identity, payload size limits, rate limits, diagnostics, and rejection records.

Implement normalized location event creation, parser fixtures, failed payload diagnostics, replay foundations, and audit logging.

Follow `docs/api/DEVICE_CONTRACTS.md`, `docs/ARCHITECTURE.md`, and `docs/SECURITY.md`.
```
