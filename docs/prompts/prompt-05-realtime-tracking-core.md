# Prompt 05: Realtime Tracking Core

```text
Implement live tracking updates.

After payload processing, publish normalized location updates immediately using Laravel Reverb for web and a Flutter-compatible WebSocket channel contract for mobile. Actual update cadence depends on tracker payload cadence.

Build backend channels/events, authorization, latest tracker state, stale/no-data detection, online/offline state, and tests.

Do not build full UI pages yet except minimal verification views if needed.
```
