# Prompt 06: Design System Finalization

```text
Finalize and implement the mTrack design system.

Use Stitch MCP for all design decisions. Generate/validate the design system from `docs/DESIGN_SYSTEM.md`, preserving the operational SaaS direction: modernized structure, teal brand accent, dense desktop admin, touch-friendly mobile, map-first tracking surfaces.

Use frontend-design skill. Convert design tokens into Tailwind theme tokens for `api/` and Flutter theme guidance for `mobile/`. Define components for shell, side nav, bottom nav, tabs, cards, tables, badges, filters, forms, modals, map overlays, charts, and empty/loading/error states.
```
